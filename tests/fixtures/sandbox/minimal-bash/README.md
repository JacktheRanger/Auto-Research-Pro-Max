# minimal-bash fixture

Smallest possible repository fixture: the run command writes a deterministic
JSON file under `outputs/` and exits 0. Used to exercise sandbox plumbing
without pulling pytest into the image.
